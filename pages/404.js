import Head from "next/head";
import style from "../styles/Error.module.scss";
import {Box, Flex, Heading, Image, Text} from "@chakra-ui/react";
import Link from "next/link";
import NiceLink from "../components/NiceLink";

export default function Custom404(props) {
    return (
        <>
            <Head>
                <title>404 Not Found | APAC</title>
                <meta name="description" content="Generated by create next app"/>
                <link rel="icon" href="/favicon.ico"/>
            </Head>
            <div className={style.wrapper}>
                <Box as={'section'} position={'relative'}>
                    <Box>
                        <Heading as={'h1'} fontSize={'3rem'}>{props.text ?? "404 Page Not Found"}</Heading>
                        <Text mt={'8px'} fontWeight={'bold'}>It must have been lost in the space</Text>
                        <Text color={'gray.600'}>Perhaps you want to head back to the&nbsp;
                            <Link href={'/'} scroll={false}><a><NiceLink>Home Page</NiceLink></a></Link>&nbsp;?</Text>
                    </Box>
                    <Image src={"/errors/error.svg"} w={'80%'} alt={"404 image"} mx={'auto'}/>
                </Box>
            </div>
        </>
    )
}